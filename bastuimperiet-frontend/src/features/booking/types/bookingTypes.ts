export interface BookingFormData {
    startDate?: string;
    endDate?: string;

    ved: number;
    doft: number;
    cleaning: boolean;

    name: string;
    email: string;
    phone: string;
    address: string;
}
