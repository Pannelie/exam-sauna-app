export interface PillProps {
    $active?: boolean;
}

export interface MainContainerProps {
    $isMobile: boolean;
}

export interface ListWrapperProps {
    $isMobile: boolean;
}

export interface DetailViewBoxProps {
    $hasId: boolean;
}
